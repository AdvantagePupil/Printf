#include "main.h"

/**
 * _stringlen - Entry point
 *
 * description: a function that calculates the length if a string
 *
 * @str: string
 *
 * Return: length
 */

int _stringlen(char *str)
{
	int i;

	for (i = 0; str[i] != 0; i++)
	{
		;
	}
	return (i);
}
